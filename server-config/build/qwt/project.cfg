[DEFAULT]
configureExecutable=qmake

[Build]
inSourceBuild=True
configureCommand=%(configureExecutable)s "INSTALL += {instPrefix}" "QWT_INSTALL_DOCS = {instPrefix}/doc" "QWT_INSTALL_HEADERS = {instPrefix}/include" "QWT_INSTALL_LIBS = {instPrefix}/lib" {sources}/qwt.pro
